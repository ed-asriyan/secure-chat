<UserControl x:Class="Chat.EditedLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Height="auto" Width="auto" Cursor="IBeam">
    <Grid>
        <TextBlock Name="TextBlock"></TextBlock>
        <TextBox Name="TextBox" FontFamily="{Binding ElementName=TextBlock, Path=FontFamily, Mode=TwoWay}" FontSize="{Binding ElementName=TextBlock, Path=FontSize, Mode=TwoWay}" Width="{Binding ElementName=Label, Path=Width, Mode=TwoWay}" Height="{Binding ElementName=TextBlock, Path=Height, Mode=TwoWay}" Foreground="{Binding ElementName=TextBlock, Mode=TwoWay, Path=Foreground}" Margin="{Binding ElementName=TextBlock, Mode=TwoWay, Path=Margin}"></TextBox>
    </Grid>
</UserControl>
